import 'package:flutter/material.dart';

class JobBoardViews extends StatelessWidget {
  const JobBoardViews({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(children: []),
    );
  }
}
